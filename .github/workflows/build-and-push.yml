name: Build and push images

on:
  push:
    branches:
      - main
  workflow_dispatch:  # also allow manual triggering from the Actions UI

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/dockercomposeproj-backend:latest
            ghcr.io/${{ github.repository_owner }}/dockercomposeproj-backend:${{ github.sha }}

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend/my-react-app
          file: ./frontend/my-react-app/Dockerfile
          target: runner
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/dockercomposeproj-frontend:latest
            ghcr.io/${{ github.repository_owner }}/dockercomposeproj-frontend:${{ github.sha }}
